import Flux from 'flux';

/*************************************************
 * DEFAULT DISPATCHER FROM FLUX OFFICIAL LIBRARY
 *************************************************/

class Dispatcher extends Flux.Dispatcher {
}

let dispatcher = new Dispatcher();

export default dispatcher;
